/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/StudentCheckIn",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

export default nextConfig;
